[build]
  command = "npm run build"
  publish = "dist"

# SPA 重定向 - 所有路径都交给 index.html 处理
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  conditions = {Language = ["en"], Runtime = ["SSG"]}
